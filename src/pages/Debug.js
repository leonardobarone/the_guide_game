import Modal from '../components/Modal';

const Debug = () => {
    return <div style={{marginTop: '60px', border: '1px solid black', height: 'calc(100vh - 140px)'}}>
        <Modal />
    </div>
}

export default Debug;